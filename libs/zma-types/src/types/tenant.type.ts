export enum UserServiceTenantStatus {
  Active = 'active',
  Suspended = 'suspended',
  Pending = 'pending',
  Archived = 'archived',
  Disabled = 'disabled',
}

export enum UserServiceTenantBillingStatus {
  Paid = 'paid',
  Overdue = 'overdue',
  Trial = 'trial',
  Cancelled = 'cancelled',
}
